import styled from 'styled-components';

import px2rem from 'utils/px2rem';

const NoteComposerActionBar = styled.div`
  margin-top: ${px2rem(10)};
  text-align: right;
`;

export default NoteComposerActionBar;
