import styled from 'styled-components';

import px2rem from 'utils/px2rem';

const NoteComposerTo = styled.div`
  margin-top: ${px2rem(11)};
  padding-left: ${px2rem(3)};
  display: flex;
  align-items: center;
`;

export default NoteComposerTo;
