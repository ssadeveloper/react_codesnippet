import styled from 'styled-components';

import px2rem from 'utils/px2rem';

const NoteComposerTextWrapper = styled.div`
  margin-top: ${px2rem(12)};
  margin-left: ${px2rem(25)};
  display: flex;
`;

export default NoteComposerTextWrapper;
