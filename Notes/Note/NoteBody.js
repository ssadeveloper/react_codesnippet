import styled from 'styled-components';
import px2rem from 'utils/px2rem';

const NoteBody = styled.div`
  margin-left: ${px2rem(61)};
  margin-bottom: ${px2rem(8)};
  line-height: ${px2rem(18)};
  position: relative;
`;

export default NoteBody;
