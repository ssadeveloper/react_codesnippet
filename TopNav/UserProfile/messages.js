import { defineMessages } from 'react-intl';

const messages = defineMessages({
  logout: {
    id: 'components.TopNav.UserProfile.action.logout',
    defaultMessage: 'Log Out',
  },
});

export default messages;
